name: Daily Stock Index Fetcher

on:
  schedule:
    # UTC 時間 00:00 對應台灣時間 08:00
    - cron: '0 0 * * *'
  # 讓你可以在網頁上手動按按鈕測試
  workflow_dispatch:

jobs:
  run-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code (下載程式碼)
        uses: actions/checkout@v3

      - name: Set up Python (設定 Python 環境)
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies (安裝套件)
        run: |
          pip install -r requirements.txt

      - name: Run script (執行並發送通知)
        env:
          # 將剛剛設定的 Secret 注入到環境變數中
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python main.py
          
      - name: Commit and Push Data (提交並保存數據)
        run: |
          # 1. 設定機器人身分 (這會顯示在 Commit 紀錄中)
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # 2. 拉取最新的遠端程式碼 (避免多人同時寫入產生衝突)
          git pull
          
          # 3. 將新產生的 csv 加入版控
          git add data/history.csv
          
          # 4. 提交變更 (如果沒有變更，例如假日沒開盤，則略過不報錯)
          git commit -m "📈 Auto-update daily stock history" || exit 0
          
          # 5. 推送回儲存庫
          git push
